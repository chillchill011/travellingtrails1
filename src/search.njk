---
layout: base
title: Search | Travelling Trails
permalink: /search/index.html
---
<div class="max-w-4xl mx-auto px-4 py-8">
    <h1 class="text-3xl font-bold text-gray-900 dark:text-white mb-8">Search Posts</h1>

    <div class="search-container mb-8">
        <div class="relative">
            <input 
                type="text" 
                id="searchInput"
                class="w-full px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-lg 
                       bg-white dark:bg-gray-800 text-gray-900 dark:text-white
                       focus:ring-2 focus:ring-travel-500 focus:border-travel-500"
                placeholder="Search posts..."
            >
            <div id="searchSuggestions" 
                 class="absolute z-10 w-full mt-1 bg-white dark:bg-gray-800 border border-gray-200 
                        dark:border-gray-700 rounded-lg shadow-lg hidden">
            </div>
        </div>

        <div class="mt-4 flex flex-wrap gap-4">
            <div class="w-full md:w-auto">
                <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">Category</label>
                <select id="categoryFilter" 
                        class="w-full md:w-48 px-3 py-2 border border-gray-300 dark:border-gray-600 
                               rounded-lg bg-white dark:bg-gray-800 text-gray-900 dark:text-white">
                    <option value="">All Categories</option>
                    {% for category in collections.categories %}
                    <option value="{{ category.title }}">{{ category.title }}</option>
                    {% endfor %}
                </select>
            </div>

            <div class="w-full md:w-auto">
                <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">Date Range</label>
                <select id="dateFilter" 
                        class="w-full md:w-48 px-3 py-2 border border-gray-300 dark:border-gray-600 
                               rounded-lg bg-white dark:bg-gray-800 text-gray-900 dark:text-white">
                    <option value="">All Time</option>
                    <option value="7">Last Week</option>
                    <option value="30">Last Month</option>
                    <option value="365">Last Year</option>
                </select>
            </div>
        </div>
    </div>

    <div id="searchResults" class="grid grid-cols-1 gap-6"></div>
    <div id="noResults" class="text-center text-gray-600 dark:text-gray-400 hidden">
        No posts found matching your criteria.
    </div>
</div>

<script src="https://unpkg.com/lunr/lunr.js"></script>
<script src="{{ '/assets/js/search.js' | url }}"></script>
