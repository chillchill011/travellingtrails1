---
layout: base
title: Home | Travelling Trails
---

<div class="max-w-7xl mx-auto">
    <header class="text-center mb-12">
        <h1 class="text-4xl font-bold text-gray-900 dark:text-white mb-4">Welcome to Travelling Trails</h1>
        <p class="text-xl text-gray-600 dark:text-gray-300">Discover amazing travel stories and adventures</p>
    </header>

    {# Featured Posts Section #}
    {% if collections.homeFeatured.length > 0 %}
    <section class="mb-16">
        <div class="flex justify-between items-center mb-8">
            <h2 class="text-2xl font-bold text-gray-900 dark:text-white">Featured Stories</h2>
            <a href="{{ '/blog/' | url }}" class="text-travel-600 hover:text-travel-700 dark:text-travel-500 dark:hover:text-travel-400 font-medium">
                View All Stories →
            </a>
        </div>
        <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
            {%- for post in collections.homeFeatured -%}
                {% include "components/blog-card.njk" %}
            {%- endfor -%}
        </div>
    </section>
    {% endif %}

    {# Recent Posts Section #}
    <section class="mb-16">
        <div class="flex justify-between items-center mb-8">
            <h2 class="text-2xl font-bold text-gray-900 dark:text-white">Recent Adventures</h2>
            <a href="{{ '/blog/' | url }}" class="text-travel-600 hover:text-travel-700 dark:text-travel-500 dark:hover:text-travel-400 font-medium">
                View All Posts →
            </a>
        </div>
        <div class="posts-container">
            <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
                {%- for post in collections.paginatedPosts | limit(9) -%}
                    {% include "components/blog-card.njk" %}
                {%- endfor -%}
            </div>
        </div>
        {% if collections.paginatedPosts.length > 9 %}
        <div class="flex justify-center mt-8">
            <button 
                class="load-more-btn bg-travel-600 text-white px-6 py-2 rounded-md hover:bg-travel-700 transition-colors duration-200"
                data-current-page="1"
            >
                Load More
            </button>
        </div>
        {% endif %}
    </section>

    {# Newsletter Section #}
    <section class="bg-gray-50 dark:bg-gray-800 rounded-xl p-8 mb-16">
        <div class="max-w-2xl mx-auto text-center">
            <h2 class="text-2xl font-bold text-gray-900 dark:text-white mb-4">Stay Updated</h2>
            <p class="text-gray-600 dark:text-gray-300 mb-6">Subscribe to our newsletter for the latest travel stories and updates.</p>
            <form class="flex flex-col sm:flex-row gap-4 justify-center">
                <input 
                    type="email" 
                    placeholder="Enter your email" 
                    class="px-4 py-2 rounded-md border border-gray-300 dark:border-gray-600 dark:bg-gray-700 dark:text-white focus:ring-2 focus:ring-travel-500 focus:border-travel-500"
                    required
                >
                <button 
                    type="submit" 
                    class="px-6 py-2 bg-travel-600 text-white rounded-md hover:bg-travel-700 transition-colors duration-200"
                >
                    Subscribe
                </button>
            </form>
        </div>
    </section>
</div>